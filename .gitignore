.env
node_modules/
.venv/
__pycache__/
.DS_Store
output/
*.egg-info/
dist/
# ...but the patched PWA app.min.js IS committed: it must override the theme's
# vendored copy at the same relative path (Jekyll site files beat theme files).
!blog-source/assets/js/dist/
!blog-source/assets/js/dist/app.min.js
build/
blog-source/_site/
blog-source/.bundle/
CLOUDFLARE_TUNNEL.md
.bundle/
*.report.html
.opencode-autopilot/

# Local wrangler cache (state/tmp — never commit)
.wrangler/

# Local dev secrets (never commit)
.dev.vars
blog-source/jekyll.log
